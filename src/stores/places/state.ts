export interface Place {
  isLoading: boolean;
  userLocation?: [number, number]; // [lng, lat]
}

/* export function state() {
  return {
    places: [] as Place[],
  };
} */

// export default state;